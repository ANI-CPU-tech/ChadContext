// TODO Day 2 State 2: Bedrock Nova Micro entity extraction (Converse API).
// Keep small + unit-testable against a canned PR payload (aws-sdk-client-mock).
export const handler = async (event: unknown): Promise<unknown> => {
  void event;
  return { status: 'todo' };
};
